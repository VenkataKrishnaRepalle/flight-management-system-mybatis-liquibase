<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.learning.fmsmybatisliquibase.dao.AirportDao">
    <resultMap id="airportResult" type="com.learning.fmsmybatisliquibase.model.Airport">
        <id property="uuid" column="uuid" javaType="java.util.UUID" jdbcType="VARCHAR"
            typeHandler="com.learning.fmsmybatisliquibase.handler.UuidTypeHandler"/>
        <result property="code" column="code"/>
        <result property="name" column="name"/>
        <result property="city" column="city"/>
        <result property="country" column="country"/>
        <result property="longitude" column="longitude"/>
        <result property="latitude" column="latitude"/>
        <result property="createdBy" column="created_by" javaType="java.util.UUID" jdbcType="VARCHAR"
                typeHandler="com.learning.fmsmybatisliquibase.handler.UuidTypeHandler"/>
        <result property="createdTime" column="created_time"/>
        <result property="updatedTime" column="updated_time"/>
    </resultMap>

    <select id="getByCode" resultMap="airportResult">
        SELECT *
        FROM airport
        WHERE code = #{code}
    </select>

    <select id="getAll" resultMap="airportResult">
        SELECT * FROM airport
    </select>

    <select id="getById" resultMap="airportResult">
        SELECT *
        FROM airport
        where uuid = #{uuid}
    </select>
    
    <insert id="insert" flushCache="true">
        INSERT INTO airport(uuid, code, name, city, country, longitude, latitude, created_by, created_time,
                            updated_time)
        VALUES (#{airport.uuid}, #{airport.code}, #{airport.name}, #{airport.city}, #{airport.country},
                #{airport.longitude}, #{airport.latitude}, #{airport.createdBy}, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
    </insert>

    <update id="update" flushCache="true">
        UPDATE airport
        SET code         = #{airport.code},
            name         = #{airport.name},
            city         = #{airport.city},
            country      = #{airport.country},
            longitude    = #{airport.longitude},
            latitude     = #{airport.latitude},
            created_by   = #{airport.createdBy},
            updated_time = CURRENT_TIMESTAMP
        WHERE uuid = #{airport.uuid}
    </update>

    <delete id="delete" flushCache="true">
        DELETE
        FROM airport
        WHERE uuid = #{uuid}
    </delete>
</mapper>